services:
  web:
    build: .
    command: bash -c "cd /app && ./bin/rails server -b 0.0.0.0"
    volumes:
      - ./app:/app
    ports:
      - "3000:3000"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: password
